const fetchingData = (state = true, action) => {
    switch (action.type) {
      case 'SEARCHED_LOCATION':
        return state
      default:
        return state
    }
  }
  
  export default fetchingData
  